---
name: manual-test
description: Generate a focused manual Obsidian test pass for Root Books Tabs
agent: plan
argument-hint: Name the feature or regression to test
---

Create a manual test pass for Root Books Tabs focused on: ${input:focus:the current changes}

Use [AGENTS.md](../../AGENTS.md) and [README.md](../../README.md) as the behavior contract.

Use `C:\Users\Admin\quartz-vaults\test-vault` as the default disposable playground.

Cover a single-folder inert vault; same/cross-book links and existing destination-tab reuse; all three global note-opening modes plus per-book overrides from settings and the pseudo-tab menu; independent main/pop-out/duplicate-instance back-forward behavior, keyboard focus, color/disabled states, same-tab forward-branch truncation, and closed-tab pruning; new-tab arrow navigation following the live left-to-right order after manual tab dragging, temporary cross-book mixing, group transfer, and menu/long-press regrouping while skipping other-book tabs; right-of-current insertion and end insertion; default close-previous and compatibility keep-open main-book switching across main groups and pop-outs; default file-explorer routing to the newest matching instance (including obscured pop-out foregrounding), compatibility current-group routing, and no history crossover; one-time fresh-clone path and reset Boolean; invalid-path fallback preserves restored tabs; root-level files; sole-file and empty-leaf replacement; right/left/up/down creation; Grid row/column slider and number-input boundaries/defaults, automatic refill after increases and decreases, multiple rectangle shapes, full-capacity overflow cycling in every direction and a visible tab bar on every overflow tile; a stable rightmost new-book-position selector with Grid overflow controls to its left; selected book always first and additions appended in logical order; managed main/pop-out recovery; same-display placement for every plugin-created pop-out; visible cross-realm pop-out book menu with whole-book pin and Return book to Obsidian; pseudo-tab whole-group dragging across each main-workspace edge, out to a pop-out, and back into main while preserving every tab, active state, and managed/free ownership; menu and long-press sorting of mixed/separated tabs into books with active-focus preservation, reduced-motion behavior, no unscoped-tab movement, managed pop-outs returned into the configured Grid; explicit free destinations and homogeneous duplicate groups whose move/return preserves free ownership; click-to-close-latest, Ctrl-click close-all, and Shift-click instance/tab picker with every live tab (including index plus overview), exact-target red dimming, no leaf focus change, and temporary obscured-pop-out foreground preview/restoration; dedicated secondary close-tree buttons that open the same picker and drag vertically to persistently reorder at least three secondary books; the red explorer Close all beside Open another book closing main and pop-out secondaries while every primary-book tab/pop-out remains untouched; missing-index entry fallback without whole-vault enumeration; note and toolbar-folder creation in current-folder/book-root modes, including the selected-book fallback with no open file; native-close return and intentional close; PNG, SVG, PDF, Canvas, Base, and deferred views; automatic color persistence and explicit frontmatter override controls and frontmatter `tab-text-bg` values in black/white/custom hex plus missing/invalid white fallback; normal Obsidian text in Underline, Colored dot, and Custom styles; inline Create missing/Regenerate all config-note action and duplicate prevention; primary-close promotion of the latest secondary, close-on-hover secondary bars, always-available launcher immediately after the primary book's complete file tree and before secondary trees, with creation, unopened books, a checked excluded-folder count action, and a larger-dotted labeled native-tree panel sliding open above the main selector with an X close control, wrapping narrow hover text and Shift-click pop-out; open every playground book, repeatedly scroll from first to last and back, close/reopen several off-screen books without restarting, and verify every native file tree reattaches with the launcher still between primary and secondary trees; complete explorer cleanup; narrow/fullscreen CSS modal Apply/Cancel; plugin disable/re-enable teardown.

For portable config and ordering, explicitly cover additive root/book config creation; preservation of unrelated YAML, YAML comments, note bodies, and colliding plain keys through `book-tabs-` aliases; direct root-config edits refreshing the UI; fresh-clone valid/invalid/book-only/empty-book paths; the visual `.obsidianignore` manager with escaped literal files and folders; a fully hidden book with create-page and session-reveal actions; checked excluded-folder count actions and a larger-dotted labeled native-tree panel above each main selector, closable through its X or the menu action and stable through book-mode toggles and file creation; excluded-file routing into one dedicated adjacent or pop-out group; new-book defaults; color-key/config-note rename migration and rollback; reading/editing context menus plus collision-safe shortcuts; recursive `fileOrder` generation and repair for every visible file type with config notes excluded and independently pinned top/bottom; three-value cycling plus ascending/descending controls; recursive folder overrides; ordinary same-name swaps; index-note move blocking and all five transfer choices; depth-aligned drag/drop feedback; temporary dragged-folder collapse; inert dimmed controls with red-X and dimmed-area exits; enter-time expansion, native collapse, and exit-time restoration. Verify inherited templates for Markdown, Canvas, Base, image, and wildcard cases, including same-date time suffixes; forced automatic link updates; regrouping duplicate-note removal; and Grid row-first fill followed by stable all-cell overflow, long-press refill, and automatic refill after both increasing and decreasing rows or columns.

For each test give setup, action, expected result, and diagnostics to capture on failure.
